void setup() {
  Serial.begin(9600);
  Serial.println("Type something:");
}

void loop() {
  if (Serial.available()) {
    char data = Serial.read();
    Serial.print("Received: ");
    Serial.println(data);
  }
}
